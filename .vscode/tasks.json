{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Tensorboard",
            "type": "shell",
            "options": { 
                "cwd": "${workspaceFolder}", 
                "env": {
                    "CUBLAS_WORKSPACE_CONFIG": ":4096:8",
                },
            },
            "command": "${command:python.interpreterPath} -c \"import tensorboard.main; tensorboard.main.run_main()\" --samples_per_plugin=SCALARS=10000 --logdir=${workspaceFolder}/runs",
            "problemMatcher": [],
        }
    ]
}